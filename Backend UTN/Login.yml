info:
  name: Login
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:3001/api/auth/login
  body:
    type: json
    data: |-
      {
        "email": "sofia@test.com",
        "password": "Password123!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
